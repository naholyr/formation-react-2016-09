import { ??? } from '???';
import { createReducer } from '../helpers';


const initialState = ???

const reducers = {

  [???]: (prevState, payload) =>
    newState,

}


export default createReducer(initialState, reducers);
